# user/validate

用户端业务校验层。

## 当前用途
- `SearchValidate`
- `CollectValidate`
- `DocValidate`
- `ApiKeyValidate`
- `LogQueryValidate`

## 目标
让用户端模块也逐步形成：
- controller
- validate
- service
- common/user
这一套统一结构。
